//optional parameter -> ? after the variableName
//Optional parameter should be at last value
function comDetails(companyName, comPhNo, options) {
    console.log(companyName + " " + comPhNo);
}
comDetails("TestLeaf", 4567890);
